description : 'Early_Fusion_HE_KI67' 

# directory to features
data_root_dir: /local/data3/chrsp39/CBTN_v2/Merged_HE_KI67/features/${feature_type}/
# experiment code to load trained models
models_exp_code: ${task}_${stain_modality.description}_${model_size}_${model_type}_${feature_type}_s1
# experiment name for saving results
save_exp_code: ${task}_${stain_modality.description}_${model_size}_${model_type}_${feature_type}

